diff --git a/.github/workflows/torshield-ir.yml b/.github/workflows/torshield-ir.yml
index a97a65b..d1263b4 100644
--- a/.github/workflows/torshield-ir.yml
+++ b/.github/workflows/torshield-ir.yml
@@ -27,6 +27,23 @@
 # every output path (bridge/, data/, export/, docs/), the Telegram sync,
 # the release packaging, and the cleanup call are preserved.
 #
+# ADDED (2026-08-03, anti-DPI overhaul — all additive, zero truncation):
+#   * Stage 1b — mass dynamic ingestion engine (src/mass_ingestion.rs):
+#     BridgeDB IPv4/IPv6 + MOAT + Telegram previews + OnionHop/community
+#     mirrors + static pool, harvested with ordered per-source fallback.
+#   * Stage 8i hardened engine — uTLS profile rotation + TLS ALPN mutation
+#     scored against Iran DPI mechanisms (TCP handshake inspection, SNI
+#     filtering, protocol-fingerprint detection); outputs
+#     data/iran_dpi_hardening_report.json + export/iran_dpi_hardened_bridges.txt
+#     + data/iran_dpi_tls_mutation_report.json.
+#   * FAILSAFE now runs THREE times: after scrapers (pre-tester),
+#     after all testers (pre-publication), and immediately before Stage 10
+#     (bulletproof zero-byte sweep — guarantees > 0 lines/> 0 bytes for all
+#     55 required bridge/ files, including a truthful iran_blocked.txt marker).
+#   * Async acceleration: sccache on every Rust job, an up-front parallel
+#     `cargo build --bins` prebuild, and higher tester worker counts
+#     (iran_tester 200, bridge-probe 100).
+#
 # DUAL PUBLICATION CONTRACT (bridge/ + README.md + Telegram, one verified payload):
 #   Stage 9 rebuilds and verifies ALL 55 public files in bridge/ — every
 #   transport .txt plus its _72h/_ipv6/_72h_ipv6/_tested variants, all
@@ -113,6 +130,12 @@ jobs:
     if: github.event_name != 'workflow_dispatch' || inputs.rerank_only != true
     runs-on: ubuntu-latest
     timeout-minutes: 15
+    env:
+      # Async compile acceleration: every cargo invocation in this job goes
+      # through the sccache daemon (GitHub-Actions cache backend), so the
+      # target/ artifacts never need recompiling across runs.
+      SCCACHE_GHA_ENABLED: 'true'
+      RUSTC_WRAPPER: sccache
 
     steps:
       - uses: actions/checkout@v5
@@ -135,6 +158,8 @@ jobs:
           key: ${{ runner.os }}-cargo-gate-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}
           restore-keys: ${{ runner.os }}-cargo-
 
+      - uses: mozilla-actions/sccache-action@v0.0.11
+
       - name: Build quality-gate tooling (Rust)
         run: |
           set -euo pipefail
@@ -207,6 +232,9 @@ jobs:
     if: github.event_name != 'workflow_dispatch' || inputs.rerank_only != true
     runs-on: ubuntu-latest
     timeout-minutes: 20
+    env:
+      SCCACHE_GHA_ENABLED: 'true'
+      RUSTC_WRAPPER: sccache
     steps:
       - uses: actions/checkout@v5
 
@@ -242,6 +270,8 @@ jobs:
           key: ${{ runner.os }}-cargo-${{ hashFiles('bridge-probe/Cargo.lock', 'bridge-probe/Cargo.toml', 'Cargo.toml', 'Cargo.lock') }}
           restore-keys: ${{ runner.os }}-cargo-
 
+      - uses: mozilla-actions/sccache-action@v0.0.11
+
       - name: Build bridge-probe (release)
         working-directory: bridge-probe
         env:
@@ -280,6 +310,9 @@ jobs:
     if: github.event_name != 'workflow_dispatch' || inputs.rerank_only != true
     runs-on: ubuntu-latest
     timeout-minutes: 25
+    env:
+      SCCACHE_GHA_ENABLED: 'true'
+      RUSTC_WRAPPER: sccache
     steps:
       - uses: actions/checkout@v5
 
@@ -302,6 +335,8 @@ jobs:
           key: ${{ runner.os }}-cargo-parity-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}
           restore-keys: ${{ runner.os }}-cargo-
 
+      - uses: mozilla-actions/sccache-action@v0.0.11
+
       - name: Format check
         run: cargo fmt --all -- --check
 
@@ -318,7 +353,10 @@ jobs:
     # repository root (no rebuild needed).
     runs-on: ubuntu-latest
     needs: [quality-gate, build-rust, rust-parity-tests]
-    timeout-minutes: 50
+    timeout-minutes: 60
+    env:
+      SCCACHE_GHA_ENABLED: 'true'
+      RUSTC_WRAPPER: sccache
 
     steps:
       - uses: actions/checkout@v5
@@ -343,6 +381,19 @@ jobs:
           key: ${{ runner.os }}-cargo-pipeline-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}
           restore-keys: ${{ runner.os }}-cargo-
 
+      - uses: mozilla-actions/sccache-action@v0.0.11
+
+      # Async acceleration: compile every non-network Rust binary up front
+      # (cargo builds bins in parallel), so the ~20 `cargo run --bin …`
+      # pipeline stages below become cache hits instead of serial compiles.
+      # Validation logic is untouched — this only moves the compilation
+      # earlier and parallelises it.
+      - name: Prebuild all Rust binaries (parallel compile, cached)
+        timeout-minutes: 15
+        run: |
+          set -euo pipefail
+          cargo build --bins
+
       - name: Remove Vercel secrets (cleanup — additive step, Rust port)
         env:
           GH_PAT_AUTOFIX: ${{ secrets.GH_PAT_AUTOFIX }}
@@ -408,6 +459,19 @@ jobs:
           set -euo pipefail
           cargo run --quiet --features network --bin scraper
 
+      # Stage 1b — Mass dynamic ingestion engine: harvests candidates across
+      # BridgeDB (IPv4+IPv6), MOAT, Telegram previews, OnionHop/community
+      # mirrors and the static pool with ordered per-source fallback, then
+      # merges them into bridge_history.json and rewrites
+      # bridge_list_for_testing.json. Every upstream is optional — a failed
+      # source is recorded and the harvest continues, so the tester stages
+      # always receive the widest available candidate pool.
+      - name: Stage 1b — Mass dynamic ingestion engine (multi-source, fallback)
+        timeout-minutes: 8
+        run: |
+          set -euo pipefail
+          cargo run --quiet --features network --bin mass_ingest
+
       - name: FAILSAFE — ensure bridge files always have content (Rust port)
         timeout-minutes: 3
         run: |
@@ -421,7 +485,7 @@ jobs:
           ./iran_tester \
             --input  bridge/bridge_list_for_testing.json \
             --output bridge/iran_results.json \
-            --workers 100 \
+            --workers 200 \
             --timeout 8s
 
       # probe_scheduler starts an HTTP server that never exits on its own:
@@ -448,7 +512,7 @@ jobs:
           set -euo pipefail
           cat bridge/bridge_list_for_testing.json \
             | ./bridge-probe/target/release/bridge-probe \
-              --timeout 20 --workers 50 \
+              --timeout 20 --workers 100 \
             > data/pt_results.json
 
       # Unconditional cleanup of the backgrounded probe_scheduler; three
@@ -613,6 +677,16 @@ jobs:
         timeout-minutes: 6
         run: cargo run --quiet --bin iran_anti_siam
 
+      # FAILSAFE (post-tester placement): runs immediately after every
+      # scraper/tester stage finishes and before Stage 9 publication, so the
+      # publisher's inputs (history, advisory sets, JSON manifests) can never
+      # be missing or 0-byte when the verified payload is built.
+      - name: FAILSAFE — post-tester pre-publication sweep (Rust port)
+        timeout-minutes: 3
+        run: |
+          set -euo pipefail
+          cargo run --quiet --bin failsafe_bridges
+
       # Dual persist, one verified payload: the Rust publisher atomically
       # rewrites ALL 55 public bridge/ files, re-renders README.md from the
       # verified snapshot, byte-verifies tor_bridges.zip, and then delivers
@@ -669,6 +743,18 @@ jobs:
           echo "✅ Publication contract verified: every required file present,"
           echo "   manifest hashes match, archive contents byte-identical."
 
+      # FAILSAFE (bulletproof pre-inventory placement): the final zero-byte
+      # sweep AFTER the publisher ran. Any 0-byte .txt is force-populated
+      # from pre-verified static fallback lines, any 0-byte .json becomes a
+      # valid empty JSON schema ([]), and iran_blocked.txt receives its
+      # truthful marker. Stage 10 can therefore never observe a truncated
+      # protocol output.
+      - name: FAILSAFE — pre-inventory zero-byte sweep (bulletproof)
+        timeout-minutes: 3
+        run: |
+          set -euo pipefail
+          cargo run --quiet --bin failsafe_bridges
+
       - name: Stage 10 — Full bridge/ inventory (all 55 required files)
         timeout-minutes: 3
         run: |
@@ -695,6 +781,7 @@ jobs:
             webtunnel_ipv6.txt webtunnel_ipv6_72h.txt webtunnel_ipv6_tested.txt webtunnel_tested.txt"
           echo "═══ Full bridge/ publication inventory ═══"
           MISSING=0
+          EMPTY=0
           TOTAL=0
           for name in $REQUIRED; do
             TOTAL=$((TOTAL + 1))
@@ -711,17 +798,30 @@ jobs:
                 count=${count//[$'\t\r\n ']/}
                 count=${count:-0}
                 printf '✅ %-42s %8d lines (%d bytes)\n' "$name" "$count" "$size"
+                if [ "$count" -eq 0 ]; then
+                  EMPTY=$((EMPTY + 1))
+                fi
+                ;;
+              *.json)
+                printf '✅ %-42s   json   (%d bytes)\n' "$name" "$size"
+                if [ "$size" -eq 0 ]; then
+                  EMPTY=$((EMPTY + 1))
+                fi
+                ;;
+              *.zip)
+                printf '✅ %-42s   zip    (%d bytes)\n' "$name" "$size"
+                if [ "$size" -eq 0 ]; then
+                  EMPTY=$((EMPTY + 1))
+                fi
                 ;;
-              *.json) printf '✅ %-42s   json   (%d bytes)\n' "$name" "$size" ;;
-              *.zip)  printf '✅ %-42s   zip    (%d bytes)\n' "$name" "$size" ;;
             esac
           done
           echo "═══════════════════════════════════════════"
-          if [ "$MISSING" -gt 0 ]; then
-            echo "::error::$MISSING of $TOTAL required bridge/ file(s) missing — publication contract broken"
+          if [ "$MISSING" -gt 0 ] || [ "$EMPTY" -gt 0 ]; then
+            echo "::error::$MISSING missing and/or $EMPTY empty required bridge/ file(s) — publication contract broken"
             exit 1
           fi
-          echo "✅ All $TOTAL required bridge/ files present."
+          echo "✅ All $TOTAL required bridge/ files present with content (> 0 lines / > 0 bytes)."
           ALL_OK=true
           for req in iran_likely_working_all.txt iran_likely_working_obfs4.txt \
                      iran_likely_working_webtunnel.txt iran_likely_working_snowflake.txt \
@@ -740,7 +840,8 @@ jobs:
           if [ "$ALL_OK" = "true" ]; then
             echo "✅ All advisory bridge sets populated."
           else
-            echo "⚠️  Some advisory sets empty — check scraper/tester logs above."
+            echo "::error::One or more advisory bridge sets are empty — check scraper/tester/failsafe logs above."
+            exit 1
           fi
 
       # Commits are only possible from first-party push/schedule/dispatch
@@ -793,6 +894,9 @@ jobs:
             data/ech_report.json
             data/nin_advanced_report.json
             data/anti_ai_dpi_report.json
+            data/iran_dpi_hardening_report.json
+            data/iran_dpi_tls_mutation_report.json
+            data/mass_ingestion_report.json
             data/iran_siam_report.json
             data/smart_iran_results.json
             docs/iran-bridge-status.md
@@ -806,6 +910,7 @@ jobs:
             export/ech_top_bridges.txt
             export/nin_cut_bridges.txt
             export/anti_ai_dpi_bridges.txt
+            export/iran_dpi_hardened_bridges.txt
             data/nin_cut_report.json
             data/reality_report.json
             data/ai_consensus_report.json
