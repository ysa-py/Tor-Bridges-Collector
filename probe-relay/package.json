{
  "name": "tor-bridge-probe-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — external probe relay for Tor bridge PT handshake verification",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
