diff --git a/.github/workflows/main-ci.yml b/.github/workflows/main-ci.yml
index 3cdcb07..0ef6cc8 100644
--- a/.github/workflows/main-ci.yml
+++ b/.github/workflows/main-ci.yml
@@ -441,12 +441,21 @@ jobs:
       - name: Restore/build Rust cache (Swatinem)
         uses: Swatinem/rust-cache@v2
 
-      - name: Add ARMv7 target
+      # ring/rustls compile a small C/assembly component even for `cargo
+      # check`; installing the ARM compiler fixes the old false assertion that
+      # no linker/C toolchain was needed. We still do not link a runnable ARM
+      # executable, so this remains a deterministic type/build verification.
+      - name: Add ARMv7 target and C compiler for ring
         run: |
           set -euo pipefail
           rustup target add armv7-unknown-linux-musleabihf
+          sudo apt-get update
+          sudo apt-get install -y gcc-arm-linux-gnueabihf
 
       - name: Run deterministic ARMv7 cargo check
+        env:
+          CC_armv7_unknown_linux_musleabihf: arm-linux-gnueabihf-gcc
+          AR_armv7_unknown_linux_musleabihf: arm-linux-gnueabihf-ar
         run: |
           set -euo pipefail
           cargo check --target armv7-unknown-linux-musleabihf --workspace --release
diff --git a/.github/workflows/torshield-ir.yml b/.github/workflows/torshield-ir.yml
index 9d5c31f..4b710ef 100644
--- a/.github/workflows/torshield-ir.yml
+++ b/.github/workflows/torshield-ir.yml
@@ -305,11 +305,14 @@ jobs:
       - name: Format check
         run: cargo fmt --all -- --check
 
-      - name: Clippy (deny warnings)
-        run: cargo clippy --workspace --all-targets -- -D warnings
+      - name: Clippy (all features, deny warnings)
+        run: cargo clippy --workspace --all-targets --all-features -- -D warnings
 
-      - name: Full Rust test suite
-        run: cargo test --workspace
+      - name: Full Rust test suite (all features)
+        run: cargo test --workspace --all-targets --all-features
+
+      - name: Release build verification
+        run: cargo build --workspace --release
 
   # ────────────────────────────────────────────────────────────────────────────
   scrape-and-test:
@@ -350,6 +353,23 @@ jobs:
           GH_REPO_NAME: ${{ secrets.GH_REPO_NAME }}
         run: cargo run --quiet --features network --bin vercel_cleanup
 
+      # IPv4 obfs4 verification is protocol-level: the collector starts this
+      # client and confirms SOCKS CONNECT after the obfs4 layer. Package names
+      # vary between Ubuntu releases, so absence is explicitly safe and the
+      # collector applies its OBFS4_VERIFY_MIN_FRACTION TCP fallback.
+      - name: Install obfs4proxy / lyrebird for protocol verification
+        run: |
+          set -euo pipefail
+          sudo apt-get update
+          sudo apt-get install -y obfs4proxy || true
+          if ! command -v obfs4proxy >/dev/null 2>&1; then
+            echo "obfs4proxy unavailable; attempting lyrebird"
+            sudo apt-get install -y lyrebird || echo "lyrebird unavailable; collector will use documented safe fallback"
+          fi
+
+      - name: Build unified collector (release)
+        run: cargo build --release --bin tor-bridges-collector
+
       - name: Stage 00 — Autonomous self-heal pipeline diagnostics
         continue-on-error: true
         timeout-minutes: 5
@@ -409,12 +429,27 @@ jobs:
         timeout-minutes: 5
         run: cargo run --quiet --features network --bin scraper
 
-      - name: Stage 0b — OnionHop bridge collection
+      # One Rust entry point merges OnionHop.py's source/protocol behavior and
+      # vip.py's history/README/ZIP/Telegram behavior. No Python collector is
+      # invoked and no capability is duplicated in a second scraper stage.
+      - name: Stage 0b — Unified OnionHop + VIP bridge collector
         continue-on-error: true
-        timeout-minutes: 5
-        run: cargo run --quiet --bin scraper
-
-      - name: Stage 0c — Legacy scraper (Telegram ZIP + README)
+        timeout-minutes: 12
+        env:
+          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
+          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
+          TELEGRAM_UPLOAD: ${{ secrets.TELEGRAM_UPLOAD }}
+          RUST_LOG: info
+        run: |
+          set -euo pipefail
+          ./target/release/tor-bridges-collector \
+            --max-workers "${MAX_WORKERS:-50}" \
+            --max-test-per-list "${MAX_TEST_PER_LIST:-600}"
+
+      # Kept as a separate broad-source enrichment pass: it consumes the
+      # collector's output and contributes pipeline-specific sources, but no
+      # longer duplicates legacy VIP collection/notification behavior.
+      - name: Stage 0c — Broad source enrichment scraper
         continue-on-error: true
         timeout-minutes: 5
         run: cargo run --quiet --bin scraper
@@ -578,9 +613,17 @@ jobs:
           GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
         run: cargo run --quiet --bin pipeline -- --stage dpi --stage anti-ai-dpi --stage rotation --report data/ai_dpi_mutator_report.json
 
+      # Bounded 64-worker pool fixes the former sequential 1,584 × 3-second
+      # path that produced the 20-minute timeout shown in IMG_20260803_195634.
+      # The module records its configured cap/worker/timeout values in the JSON
+      # report, so a future regression is visible rather than silently hanging.
       - name: Stage 8k — NIN internet-cut survivability tester
         continue-on-error: true
-        timeout-minutes: 20
+        timeout-minutes: 8
+        env:
+          NIN_CUT_MAX_WORKERS: '64'
+          NIN_CUT_MAX_BRIDGES: '2000'
+          NIN_CUT_TIMEOUT_SECS: '3'
         run: cargo run --quiet --bin pipeline -- --stage nin-cut --report data/nin_cut_pipeline_report.json
 
       - name: Stage 8l — XTLS-Reality VLESS config generator
